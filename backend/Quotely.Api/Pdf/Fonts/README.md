Drop `.ttf` files here to brand the quotation PDF.

`PdfFonts` registers every TTF in this folder at startup and switches the document typeface to
"Noto Sans". With no fonts present, the PDF uses QuestPDF's built-in Lato, which already renders
the currency symbols Quotely uses (₹, $, €, £).
